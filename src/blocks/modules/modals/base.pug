
mixin modal(type, title, needReg)
    .modal.modal__default.js-modal(
        class=`modal__${type}`
        data-modal-type=type
    )&attributes(attributes)
        .modal__inner
            .close.modal__close.js-modal-close
            .modal__head
                .modal__title=title
                if needReg
                    a(href='registration.html').modal__link Регистрация
            .modal__content
                block